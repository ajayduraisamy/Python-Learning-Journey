# DL Day 27 — GPT Training with BPE Tokenizer

This project:
- Integrates BPE tokenizer
- Uses subword tokens
- Trains GPT-RoPE with BPE
- Generates text tokens

This is a **real GPT-style pipeline**.
