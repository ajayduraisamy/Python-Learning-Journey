# DL Day 4 — PyTorch Basics (XOR)

This project implements XOR learning using PyTorch:

- Custom `nn.Module`
- Sigmoid activations
- BCELoss
- Adam optimizer
- 5000 training epochs
- Saves model as xor_pytorch.pth

Run training:
