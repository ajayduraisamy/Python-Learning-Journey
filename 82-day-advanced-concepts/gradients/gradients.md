# Gradient Problems

### Vanishing gradients
Small gradients → cannot update layers.

### Exploding gradients
Huge gradients → weights blow up → NaN.

### Fixes:
- ReLU, LeakyReLU
- BatchNorm
- Residual Connections (ResNet)
