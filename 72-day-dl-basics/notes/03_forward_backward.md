# Forward & Backward Pass

Forward Pass:
    input → weights → activation → output

Loss Function:
    measures how wrong prediction is

Backward Pass:
    compute gradients
    update weights using gradient descent
