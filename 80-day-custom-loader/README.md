# DL Day 9 — Custom Image Dataset Loader

This project includes:
- Load images directly from folders
- Automatic train/validation split
- tf.data pipeline (shuffle, batch, prefetch)
- Normalization layer
- Test script to verify dataset loading

Dataset format:
dataset/
    class1/
    class2/
    ...

To test loading:
